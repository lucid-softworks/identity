# `@lucid-softworks/identity`

Return a value unchanged while preserving its exact type.

```ts
import { identity } from "@lucid-softworks/identity";

const values = [0, 1, 2, 3];
values.filter(identity);
```
